<%= form_for [:admin, :litter, @pupper] do |f| %>
  <%= f.label :gender %>
  <%= f.select :gender, [['Male', 'm'], ['Female', 'f']]%>
  <%= f.label :image %>
  <%= f.file_field   :image%>
  <%= f.submit %>
<% end %>

<%#FIXME -  ADD LITTER%> 